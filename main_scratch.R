library(stylo)
a = c(1,2,3,4,5,6,7,8)
print(a*a)
plot(a*a, col="red")

#hov to run stylo

library(stylo) #import library
setwd("***\r-stylometry\Corpuses\corpus_a") # set the working directory one upper before corpus
stylo() # run stylo

#The subdirectory "corpus" should contain at least two text samples!